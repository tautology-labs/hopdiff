{
  "name": "hopdiff",
  "version": "0.1.0",
  "description": "Call-graph diffs for code review — see what changed structurally, not just textually",
  "type": "module",
  "bin": {
    "hopdiff": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "tsc && node --test dist/*.test.js",
    "prepublishOnly": "npm test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tautology-labs/hopdiff.git"
  },
  "keywords": [
    "code-review",
    "call-graph",
    "diff",
    "ai-code-review",
    "mcp",
    "incident-response"
  ],
  "dependencies": {
    "java-parser": "^3.0.1",
    "typescript": "^5.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
